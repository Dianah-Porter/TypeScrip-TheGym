var age = 36;
// Check if the person is an adult
var result = (age >= 18) ? "Adult" : "Not Adult";
console.log(result);
// Output: Adult
var a = 3;
var res = (a >= 0) ? (a == 0 ? "zero" : "positive") : "negative";
console.log("The number is ".concat(res, "."));
